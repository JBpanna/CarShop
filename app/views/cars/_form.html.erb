<%= form_for(@car) do |f| %>
  <% if @car.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@car.errors.count, "error") %> prohibited this car from being saved:</h2>

      <ul>
      <% @car.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :Vehicle_ID %><br>
    <%= f.number_field :Vehicle_ID %>
  </div>
  <div class="field">
    <%= f.label :engine_type %><br>
    <%= f.text_field :engine_type %>
  </div>
  <div class="field">
    <%= f.label :is_four_door %><br>
    <%= f.check_box :is_four_door %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
